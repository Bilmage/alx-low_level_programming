#include "main.h"
/**
 * main - Program that prints the last digit of a number.
 *
 * Return: Always 0.
 */

int print_last_digit(int)
{
int last_digit = number % 10;
printf("Last digit: %d\n", last_digit);
return (last_digit);
    
}
